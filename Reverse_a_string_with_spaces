#include <stdio.h>
#include<string.h>
int main()
{
    int i,j,l;
    char str[50];
    scanf("%[^\n]s",str);
    l=strlen(str);
    for(i=l;i>=0;i--)
    {
     printf("%c",str[i]);    
    }
    

    return 0;
}
